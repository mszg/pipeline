Place the ONT run folders here (or use absolute paths in config/samples.tsv).

Each fastq_input may be either:
  1) a directory containing any number of .fastq / .fastq.gz files (searched recursively), or
  2) one individual .fastq / .fastq.gz file.

Mixed compressed and uncompressed FASTQs are supported.
